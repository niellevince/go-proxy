services:
  proxy:
    build: .
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    environment:
      PORT: ${PORT:-8000}
    volumes:
      - ./proxies.json:/app/proxies.json:ro
    restart: unless-stopped
